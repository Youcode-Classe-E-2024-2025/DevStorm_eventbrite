<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Codes Promo | DevEvents</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-custom-purple mb-8">Codes Promo</h1>
        
        <a href="/event/{{ event_id }}/promocode/create" 
           class="inline-block mb-8 bg-custom-purple text-white px-4 py-2 rounded-lg hover:bg-purple-700">
            Créer un nouveau code
        </a>

        <div class="bg-gray-900 rounded-lg p-6">
            {% if promoCodes|length > 0 %}
                {% for code in promoCodes %}
                    <div class="bg-black p-4 rounded-lg mb-4 flex justify-between items-center">
                        <div>
                            <h3 class="font-bold">{{ code.code }}</h3>
                            <p class="text-gray-400">{{ code.discount_percentage }}% de réduction</p>
                            <p class="text-sm text-gray-500">
                                Valide du {{ code.valid_from|date('d/m/Y') }} au {{ code.valid_until|date('d/m/Y') }}
                            </p>
                        </div>
                    </div>
                {% endfor %}
            {% else %}
                <p class="text-center text-gray-400">Aucun code promo créé pour cet événement</p>
            {% endif %}
        </div>
    </div>
</body>
</html>