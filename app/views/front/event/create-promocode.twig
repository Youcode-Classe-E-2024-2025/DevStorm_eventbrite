<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Créer un Code Promo | DevEvents</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .bg-custom-purple {
            background-color: #8B5CF6;
        }
        .text-custom-purple {
            color: #8B5CF6;
        }
    </style>
</head>
<body class="bg-black text-white">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold text-custom-purple mb-8">Créer un Code Promo</h1>
        
        <form method="POST" class="max-w-lg space-y-6">
            <div class="bg-gray-900 p-6 rounded-lg space-y-4">
                <div>
                    <label class="block mb-2">Réduction (%)</label>
                    <input type="number" name="discount_percentage" min="1" max="100" required 
                           class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg">
                </div>
                <input type="number" 
       name="discount_percentage" 
       min="1" 
       max="100" 
       required 
       class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg">

                <div>
                    <label class="block mb-2">Valide du</label>
                    <input type="datetime-local" name="valid_from" required 
                           class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg">
                </div>
                
                <div>
                    <label class="block mb-2">Valide jusqu'au</label>
                    <input type="datetime-local" name="valid_until" required 
                           class="w-full px-4 py-2 bg-gray-800 border border-gray-700 rounded-lg">
                </div>
                
                <input type="hidden" name="event_id" value="{{ event_id }}">
                
                <button type="submit" 
                        class="w-full bg-custom-purple text-white py-2 rounded-lg hover:bg-purple-700">
                    Créer le code promo
                </button>
            </div>
        </form>
    </div>
</body>
</html>
